using UnityEngine;
using System.Collections;

public class SampleChunkManager : ChunkManager
{
	public SampleChunkManager(ChunkLoader loader, ChunkUnloader unloader, ChunkMeshBuilder meshBuilder)
		: base(loader, unloader, meshBuilder)
	{

	}
}
